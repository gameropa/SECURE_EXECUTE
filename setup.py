from setuptools import setup, find_packages

setup(
    name='safe_execute',
    version='0.1.0',
    description='A decorator for safe function execution with logging and timing',
    author='king704spider',
    packages=find_packages(),
    install_requires=[],
    python_requires='>=3.7',
)
